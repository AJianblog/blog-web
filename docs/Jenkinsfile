pipeline {
    agent any

    stages {
        stage('pull code') {
            steps {
                checkout([$class: 'GitSCM', branches: [[name: '*/master']], doGenerateSubmoduleConfigurations: false, extensions: [], submoduleCfg: [], userRemoteConfigs: [[url: 'https://gitee.com/hzjanger/my-blog.git']]])
            }
        }

        stage('package project') {
            steps {
                nodejs('node15.4.0') {
                    // some block
                    sh 'npm -v'
                }
            }
        }
    }
}